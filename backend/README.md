DisasterAid Backend (Phase 1)
Structure created with sample files.

To run:
1. npm install express mongoose bcryptjs jsonwebtoken cookie-parser dotenv express-async-handler
2. npm i -D nodemon
3. Copy .env.example to .env and fill values.
4. npm run dev
